/** @type {import('tailwindcss').Config} */
export default {
  // In Tailwind CSS v4, content paths are auto-detected.
  // This file is kept for potential plugin configuration.
  // Theme customization is now in src/index.css via @theme directive.
  plugins: [],
};
